libnbcompat is a portable NetBSD-compatibility library that supplies routines
used by NetBSD bootstrap tools that are missing on other operating systems.

This project is an attempt to simply make libnbcompat more portable using modern
version of autoconf and libtool.

URL for this project:
    https://github.com/archiecobbs/libnbcompat

URL for original NetBSD sources:
    https://cdn.netbsd.org/pub/pkgsrc/current/pkgsrc/pkgtools/libnbcompat/
